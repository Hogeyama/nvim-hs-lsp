name:                nvim-hs-lsp
version:             0.1.0.0

custom-setup:
  dependencies:
    - base >= 4.7 && < 5
    - Cabal
    - cabal-doctest >= 1 && < 1.1

dependencies:
- base >= 4.7 && < 5

library:
  source-dirs: src
  ghc-options:
  - -Wall
    #- -fprint-explicit-kinds
  dependencies:
  - nvim-hs >= 0.2.5
    # 0.2.4の testWithEmbeddedNeovim は壊れている模様
  - aeson
  - constraints
  - containers
  - unordered-containers
  - extensible
  - hashable
  - hslogger
  - lens
  - process
  - safe
  - singletons
  - text
  - stm
  - bytestring
  - mtl
  - async
  - exceptions
  - extra
  - xformat

    #executables:
    #  tmp:
    #    main:                Main.hs
    #    source-dirs:         app
    #    ghc-options:
    #    - -threaded
    #    - -rtsopts
    #    - -with-rtsopts=-N
    #    - -Wall
    #    dependencies:
    #    - nvim-hs >= 0.2.2
    #    - nvim-hs-lsp
    #    - stm
    #    - process
    #    - lens

tests:
  doctests:
    source-dirs:   test
    main:          doctests.hs
    other-modules:
      - Build_doctests
    dependencies:
      - doctest

